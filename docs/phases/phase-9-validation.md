# Phase 9 Validation Log

## Metadata

- Phase: `9`
- Owner: `Engineering`
- Status: `DONE`

## Validation checklist (current slice)

- [x] `fr-ext` parser/scanner tests passing.
- [x] FFI ext probe/listing tests passing.
- [x] .NET deterministic status tests added and passing.
- [x] UI probe path includes ext between ReFS and FAT.

## Evidence

- `cargo test -p fr-ext` passed (`10` tests, includes inode-type gating, deletion-time sanity, 64-bit size parsing, multi-group inode-table coverage, and deleted directory-entry to inode-metadata linkage).
- `cargo test -p fr-ffi` passed (`37` tests, includes ext direct/single/double/triple-indirect recovery, extent-leaf recovery, sparse/uninitialized extent zero-fill semantics, inline/non-inline symlink recovery, and directory inode export).
- `dotnet test ui/windows-app/tests/FileRecovery.WindowsApp.Tests/FileRecovery.WindowsApp.Tests.csproj -c Debug` passed (`39` tests).
- `dotnet build tools/benchmarks/ExtCorpusBench/ExtCorpusBench.csproj -c Release` passed.
- UI recovery flow now executes ext direct/single/double/triple-indirect and extents-tree recovery (including sparse zero-fill handling), inline/non-inline symlink export, and directory inode byte export for supported candidates, and returns status `91` for unsupported candidates.
- ext benchmark corpus scaffolding is in place (`testdata/raw-images/ext-corpus` + benchmark script/tooling).
- ext primary corpus manifest case matrix is expanded for sparse-extents, symlink-heavy, and inode-linked-name scenarios.
- ext host/image validation scaffolding is now available via `scripts/run-host-ext-image-validation.ps1` and integrated in profile orchestration/comparison scripts.
- ext candidate metadata reconstruction now links deleted directory-entry names to deleted inode-table metadata when inode references are preserved.
- committed synthetic ext mini-corpus images are now generated by `scripts/generate-ext-synthetic-corpus.ps1` and benchmarked via `scripts/benchmark-ext-synthetic-corpus.ps1` + `manifest.synthetic.json`.
- host/profile validation entrypoints now support synthetic ext corpus mode (`-UseSyntheticCorpus` / `-UseSyntheticExtCorpus`) for deterministic in-repo validation runs.

## Deferred follow-up (non-blocking)

- Populate the expanded real-world ext corpus case matrix with additional fixture images beyond the committed synthetic mini-corpus.
